# Existing Docs Conflict Fixture

This fixture represents a project that already has governance and tool-entry documents.

It exists to verify that the skill asks the user to merge, overwrite, or skip existing files instead of silently replacing them.
