# Minimal CLI Fixture

This fixture represents a small command-line project with no API, database, deployment, or maintenance signals.

It exists to verify that the governance skill does not over-detect dimensions from a minimal JavaScript project.
